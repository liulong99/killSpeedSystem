<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccgydx.spring.boot.speed.kill.system.mapper.OrderInfoMapper">

  <insert id="insertMiaoshaOrder" parameterType="com.ccgydx.spring.boot.speed.kill.system.domain.MiaoshaOrder">
    insert into miaosha_order(user_id,goods_id,order_id) values (#{userId},#{goodsId},#{orderId})
  </insert>

</mapper>